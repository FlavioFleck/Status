export default class Appointment {
    constructor(name, serviceId, userId, date) {
        this.name = name
        this.serviceId = serviceId
        this.userId = userId
        this.date = date
    }
}